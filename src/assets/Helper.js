// Fallback base URL for the backend (used when Vite env var `VITE_APP_URL` is not set).
// Prefer configuring `VITE_APP_URL` via a `.env` file or your deployment environment.
export const DEFAULT_API_BASE_URL = 'https://annagat-admin-backend.onrender.com';
